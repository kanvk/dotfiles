[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"

# Optional: Nix — enable by installing Nix with the determinate-systems installer
# [ -e "$HOME/.nix-profile/etc/profile.d/nix.sh" ] && . "$HOME/.nix-profile/etc/profile.d/nix.sh"
