# Flavors are managed by `ya pkg` (see package.toml); the install script
# materializes them under ~/.config/yazi/flavors/. Yazi picks dark or light
# based on the detected system color-scheme preference.
[flavor]
dark  = "catppuccin-macchiato"
light = "catppuccin-latte"
