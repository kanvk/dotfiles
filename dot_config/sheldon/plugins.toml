# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[templates]

defer = "{{ hooks?.pre | nl }}{% for file in files %}zsh-defer source \"{{ file }}\"\n{% endfor %}{{ hooks?.post | nl }}"

[plugins]

[plugins.zsh-defer]
github = "romkatv/zsh-defer"

[plugins.oh-my-zsh]
github = "ohmyzsh/ohmyzsh"

# OMZ plugins loaded via sheldon with deferred sourcing instead of through
# OMZ's `plugins=(...)` array. Each one is a simple function/alias bundle
# that does not need to be available the instant the prompt appears, so
# pushing them off the critical-path keeps shell startup snappy. The dir
# directives reference the same OMZ clone sheldon already maintains for the
# oh-my-zsh entry above (sheldon de-dupes by github URL).
[plugins.omz-extract]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/extract"
use = ["extract.plugin.zsh"]
apply = ["defer"]

[plugins.omz-copypath]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/copypath"
use = ["copypath.plugin.zsh"]
apply = ["defer"]

[plugins.omz-copyfile]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/copyfile"
use = ["copyfile.plugin.zsh"]
apply = ["defer"]

[plugins.omz-history]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/history"
use = ["history.plugin.zsh"]
apply = ["defer"]

[plugins.omz-jsontools]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/jsontools"
use = ["jsontools.plugin.zsh"]
apply = ["defer"]

[plugins.omz-rsync]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/rsync"
use = ["rsync.plugin.zsh"]
apply = ["defer"]

[plugins.omz-colorize]
github = "ohmyzsh/ohmyzsh"
dir = "plugins/colorize"
use = ["colorize.plugin.zsh"]
apply = ["defer"]

[plugins.alias-tips]
github = "djui/alias-tips"
apply = ["defer"]

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"
apply = ["defer"]

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
use = ["{{ name }}.zsh"]
apply = ["defer"]

[plugins.zsh-syntax-highlighting]
github = "zsh-users/zsh-syntax-highlighting"
apply = ["defer"]

[plugins.zsh-completion-generator]
github = "RobSis/zsh-completion-generator"
apply = ["defer"]

[plugins.zsh-ssh]
github = "kanvk/zsh-ssh"
apply = ["defer"]

[plugins.fzf-tab]
github = "Aloxaf/fzf-tab"
apply = ["defer"]

[plugins.zsh-vi-mode]
github = "jeffreytse/zsh-vi-mode"
apply = ["defer"]

[plugins.forgit]
github = "wfxr/forgit"
use = ["forgit.plugin.zsh"]
apply = ["source"]

[plugins.powerlevel10k]
github = "romkatv/powerlevel10k"
